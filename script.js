const state={category:"all",query:""};let products=[];const container=document.getElementById("products-container");const tabs=document.querySelectorAll(".tab");const search=document.getElementById("product-search");const clear=document.getElementById("clear-search");const reset=document.getElementById("reset-filters");const count=document.getElementById("results-count");const mobileCount=document.getElementById("mobile-count");const updated=document.getElementById("updated-label");
const badgeClass=b=>({Bestseller:"badge-bestseller","Well Reviewed":"badge-bestseller",Popular:"badge-hot",Trending:"badge-hot","Good Value":"badge-deal","Smart Pick":"badge-deal",New:"badge-new","Wired Pick":"badge-bestseller"})[b]||"badge-deal";
function getCards(){return [...container.querySelectorAll(".product-card")]}
function render(){const q=state.query.trim().toLowerCase();let visible=0;document.querySelectorAll(".category-section").forEach(section=>{const cat=section.id;let matches=0;section.querySelectorAll(".product-card").forEach(card=>{const text=card.textContent.toLowerCase();const ok=(state.category==="all"||state.category===cat)&&(!q||text.includes(q));card.hidden=!ok;if(ok){matches++;visible++}});section.hidden=matches===0;});count.textContent=visible?visible+" curated picks":"No picks found";mobileCount.textContent=visible+" "+(visible===1?"pick":"picks");reset.style.display=(state.category!=="all"||q)?"block":"none";clear.style.display=q?"block":"none"}
function setup(){tabs.forEach(tab=>tab.addEventListener("click",()=>{tabs.forEach(t=>t.classList.remove("active"));tab.classList.add("active");state.category=tab.dataset.category;render();}));search.addEventListener("input",e=>{state.query=e.target.value;render()});clear.addEventListener("click",()=>{search.value="";state.query="";search.focus();render()});reset.addEventListener("click",()=>{state.category="all";state.query="";search.value="";tabs.forEach(t=>t.classList.toggle("active",t.dataset.category==="all"));render()})}
fetch("data/products.html").then(r=>{if(!r.ok)throw new Error("Products unavailable");return r.text()}).then(html=>{container.innerHTML=html;updated.textContent="Fresh shortlist";setup();render();}).catch(()=>{container.innerHTML='<div class="empty-state">We could not load the picks right now. Please refresh and try again.</div>';count.textContent="Picks unavailable";setup()});
document.getElementById("year").textContent=new Date().getFullYear();
